# 로봇 기획 — 이 세계에서 기계가 맡는 자리

이 문서는 **역할**을 정합니다. 어떻게 걷는지는 `보행_로봇.md`, 어떻게 생겼는지는
`Art/Blender/*_README.md` 에 있습니다.

## 지금 있는 것 (2026-09-10 실측)

| | 4족 보행기 | 드레드노트 | 스트라이더 | 노마드 |
|---|---|---|---|---|
| 몸통 높이 | 0.42 m | 1.30 m | 5.60 m | 24.0 m |
| 전체 높이 | — | 3.46 m | 약 9 m | 36.9 m |
| 질량 | 320 kg | 900 kg | 2,000 kg | 미정 (부피비로 약 130 t) |
| 다리 | 4 | 2 | 3 | 3 |
| 무장 | 없음 | 연장포 · 주먹 | 중포/연장포 (교체) | 크레인 |

넷 다 `WalkerRobot` 하나로 걷고, `RobotCombatant` 로 전투 규약(`IDamageable`·`IHostile`)에
걸려 있습니다. **없는 것**은 지각·순찰로·시각표·배치·세이브입니다. `RobotDriver` 는
"트랜스폼 하나를 향해 걸어간다" 가 전부이고, 씬에 놓인 로봇은 아직 없습니다.

## 규칙 하나 — 이것만 지키면 나머지는 따라옵니다

> **귀신은 당신을 노립니다. 로봇은 당신을 모릅니다.**

이 게임에는 이미 적대자가 있습니다. 귀신은 **밤의 것**이고, 달라붙고, 만져지지 않고,
앙크라는 의례 도구로 물러갑니다. 로봇을 "체력 있는 또 다른 적" 으로 만들면 그 축이
흐려지고 앙크는 그냥 무기가 됩니다.

메가스트럭쳐 생성기가 스스로 적어 둔 정의가 답을 줍니다 — **틀은 그것이 나르는 단위보다
오래 삽니다.** 사람이 떠난 뒤에도 도시는 자기 정비를 계속하고 있습니다. 로봇은
**아직 자기 일을 하고 있는 기반 시설**이지 사냥꾼이 아닙니다.

그래서 도구가 갈라집니다.

| 도구 | 상대 |
|---|---|
| 앙크 | 귀신 **전용** (의례) |
| 차 | 로봇 (질량 대 질량) |
| 손 · E | 로봇 (싣고, 돌리고, 비켜 주고) |

**적대적인 로봇은 딱 한 종류만 둡니다.** 그래야 그것이 나왔을 때 뜻이 생깁니다.

### 이 규칙이 실제 루프에 무엇을 더하는가

지금 주행의 긴장은 **"차를 살려서 돌아온다"** 하나뿐입니다. 귀신이 붙고, 운전하면서
털어내고, 도착합니다. 축이 하나입니다.

로봇은 두 번째 축을 줍니다 — **길 자체가 변합니다.** 순찰기가 아홉 시에 지나가고,
큰 것이 도로를 가로질러 밟고 지나가는 동안 길이 막히고, 운반기가 내 차선을 걷고
있습니다. 어느 것도 나를 쫓지 않지만 **경로와 시각을 고르게 만듭니다.** 운전 게임에서
좋은 것은 이쪽이고, 새 전투가 하나도 필요 없습니다.

덤이 하나 더 있습니다. 지금 메가스트럭쳐는 **경치**입니다. 그것을 정비하는 기계,
그 길이를 걷는 기계, 캡슐을 꽂는 기계가 있어야 모델이 **장소**가 됩니다.

## 넷의 자리 — 크기 하나에 관계 하나

| 기계 | 사는 곳 | 관계 | 하는 일 | 싸우는가 |
|---|---|---|---|---|
| 4족 보행기 | 마을 | **이웃** | 상점·창고·차 사이로 물건을 나릅니다 | 아니오 |
| 드레드노트 | 현장 | **규칙** | 현장을 지킵니다. 손대면 반응합니다 | **이것만** |
| 스트라이더 | 길 | **교통** | 정해진 길을 정해진 시각에 걷습니다 | 건드리면 |
| 노마드 | 스파인 | **날씨** | 순회합니다. 어디서나 보입니다 | 아니오 |

**이웃 → 규칙 → 교통 → 날씨.** 크기가 커질수록 관계가 멀어지고, 멀어질수록 싸울 수
없게 됩니다. 넷 다 쫓아오지 않습니다.

- **드레드노트가 이 게임의 유일한 로봇 적입니다.** 보딩·터널전 패턴이라 낮고 땅딸막하고
  근접이며, 육중화 뒤로 사람 눈높이에서 가장 무거운 물건입니다. **가만히 서 있다가
  규칙을 어겼을 때만** 움직이므로 **싸울지 말지를 플레이어가 고릅니다.**
- **노마드는 시계입니다.** 스파인을 따라 정해진 순회를 돌므로, 어디서 보든 그 위치가
  "지금 몇 시쯤이고 나는 어디쯤인가" 를 말해 줍니다. 체력은 부피비로 약 13,000 —
  못 죽이는 것이 아니라 **죽일 생각이 안 드는** 수치입니다.

## 더 필요한 것 셋 — 각각 지금 비어 있는 시스템 하나를 맡습니다

새 기계는 **지금 아무도 안 맡고 있는 구멍**을 메울 때만 만듭니다.

### 1. 정비 팔 — 마을 · 고정 · 다리 없음

| | |
|---|---|
| 메우는 구멍 | **차를 고쳐 주는 것이 하나도 없습니다.** 귀신이 내구도를 깎는데 회복 경로가 없습니다 |
| 붙는 시스템 | `VehicleHealth` · `Powertrain` 의 연료 · `Wallet` · 앞으로의 의뢰 |
| 만드는 비용 | **낮음.** `RobotTurret`(선회·부앙) 두 단을 받침대에 올리면 끝입니다. 보행 리그 불필요 |

마을에는 지금 상점과 침대가 있지만 **거점일 이유**가 없습니다. 차를 고쳐 주는 기계가
있으면 돌아가는 일에 뜻이 생기고, `WorldLocation` 이 "앞으로의 의뢰의 전제" 라고 적어 둔
그 의뢰를 **말하는 사람 없이** 나눠 줄 자리가 여기입니다.

### 2. 견인기 — 길 · 스트라이더 급 · 비무장

| | |
|---|---|
| 메우는 구멍 | **실패한 뒤가 없습니다.** 연료가 0 이 되고 내구도가 0 이 되면 그다음이 없습니다 |
| 붙는 시스템 | `Powertrain` 연료 0 · `VehicleHealth` 0 · `Wallet`(견인비) |
| 만드는 비용 | **중간.** 스트라이더의 총 자리에 갈고리를 답니다. 무장 교체는 이미 됩니다 |

죽은 차를 가지러 오는 기계가 있으면 **실패가 리로드가 아니라 사건**이 됩니다. 돈을 받아
가므로 경제도 그때 처음으로 아픕니다.

### 3. 점등기 — 길 · 작음 · 여럿

| | |
|---|---|
| 메우는 구멍 | **로봇과 귀신이 서로 무관합니다.** 두 시스템이 나란히 있을 뿐 곱해지지 않습니다 |
| 붙는 시스템 | `IGhostActivity` — `GhostSpawner` 가 이미 주입받는 배율입니다 |
| 만드는 비용 | **낮음.** 4족 보행기 섀시에 등대를 세웁니다 |

길의 등을 다시 켜며 걷는 기계입니다. **불이 켜진 구간에서 귀신 활동 배율이 내려갑니다** —
`GhostSpawner` 는 이미 `IGhostActivity` 를 주입으로 받고 있으므로 **훅 하나면 됩니다.**

이것이 셋 중 유일하게 **플레이어가 로봇을 지키고 싶어지게** 만듭니다. 나머지 기계는
전부 비켜 주거나 부수는 대상입니다.

### 만들지 않기로 한 것

- **실내용 기계.** 캡슐과 방이 있지만, 실내 조우는 귀신과 자리가 겹칩니다.
- **나는 것 · 매달린 것.** `WalkerRobot` 은 다리 전용이고 비행은 새 시스템입니다.
- **로봇을 귀신처럼 스폰하기.** 로봇은 **장소에 속합니다.** 씨앗으로 배치하지, 차 뒤에
  뽑아내지 않습니다. 이것이 `GhostSpawner` 를 흉내 내면 안 되는 이유입니다.

## 그러려면 만들어야 하는 것 (작업 순서)

| # | 무엇 | 왜 먼저인가 |
|---|---|---|
| 1 | **배치** — 장소별 · 시드 고정 | 세계에 없으면 나머지가 전부 무의미합니다. `WorldStreamer` 가 하는 방식 그대로 |
| 2 | **순찰로 + 시각표** | `ShopSchedule` 이 이미 시각표의 선례입니다. `TimeSystem` 에서 읽습니다 |
| 3 | **지각** — 알아챔 · 무시 · 항의 | 지킴이와 순찰기에 필요합니다. 셋 중 **"무시" 가 기본값**입니다 |
| 4 | 정비 팔 | 새 기계 중 가장 싸고 루프에 가장 크게 붙습니다 |
| 5 | 견인기 | 실패에 뒤가 생깁니다 |
| 6 | 점등기 + 빛–귀신 훅 | 유일한 새 메커닉이므로 마지막에 겁니다 |

**세이브도 함께 봐야 합니다.** `SaveRegistry`·`*SaveParticipant` 규약이 이미 있으므로,
쓰러뜨린 로봇은 쓰러진 채로 남아야 합니다 — 지형이 파괴되지 않고 돌아갈 마을이 남는
것과 같은 이유이고, `보행_로봇.md` 가 이미 `stayDownOnDeath` 로 그렇게 정해 두었습니다.

## 앙크와 로봇 — 지금 상태와 제안

지금은 `RobotCombatant` 때문에 **앙크로 로봇을 때릴 수 있고**, `RobotCombatantTests` 가
그것을 검증합니다. 위의 규칙과 충돌합니다.

**깨지 않고 지키는 방법**은 인터페이스를 떼는 것이 아니라 **수치로 말하는 것**입니다.
체력이 질량 1 kg 당 0.1 이므로 드레드노트 90 · 스트라이더 200 인데, 앙크는 초당 피해라
지금은 앙크가 답이 됩니다. 기계 상대 피해에 배율(예: 0.1)을 두면 **앙크로도 되긴 하지만
차로 받는 편이 스무 배 빠릅니다.** 테스트는 그대로 통과하고, 의례 도구의 자리도 지킵니다.

## 아직 결정하지 않은 것

- **노마드의 질량.** 부피비로 130 t 이지만, 물리 상호작용이 실제로 필요한지부터
  정해야 합니다. 못 밀 물건이면 `RobotPhysicsMotor` 없이 두는 편이 쌉니다.
- **현장이 몇 개이고 무엇이 있는가.** 지킴이의 규칙은 "지킬 것" 이 정해져야 씁니다.
- **로봇이 귀신에게 어떻게 보이는가.** 지금은 서로 없는 셈입니다. 점등기 훅이
  그 관계의 첫 줄이 될지, 아니면 귀신이 기계에도 달라붙을지는 열려 있습니다.

관련: `보행_로봇.md` · `Art/Blender/Nomad_README.md` · `Art/Blender/Dreadnought_README.md`


---

# 실행 — 순찰로와 시각표 (2026-09-11)

작업 순서 2번입니다. `RobotDriver` 는 "트랜스폼 하나를 향해 걸어간다" 가 전부라,
씬에 놓인 로봇은 제자리에 서 있거나 한 점으로 걸어가서 멈췄습니다.
그것은 기계가 아니라 **장식**입니다.

## 스트라이더만 줍니다

"넷의 자리" 가 이미 갈라 두었습니다.

* **스트라이더는 교통** — "정해진 길을 정해진 시각에 걷습니다". 이 부품의 자리입니다.
* **드레드노트는 규칙** — "가만히 서 있다가 규칙을 어겼을 때만" 움직입니다.
  ⚠ **파수꾼에게 순찰로를 주면 그 자리가 무너집니다.** 설치 도구가 이름으로 가릅니다.

## 시각표는 마트와 같은 규칙입니다

`ShopSchedule.IsWithinHours` 와 같습니다 — 여는 시각과 닫는 시각이 **같으면 24 시간**,
닫는 시각이 더 이르면 자정을 넘긴 근무. 마트가 이미 그렇게 돌고 있으므로 여기서
다른 규칙을 만들 이유가 없습니다. 이 게임은 밤이 본편이라 **자정을 넘긴 근무가
기본**이고, 그래서 그 경우에 테스트를 따로 두었습니다.

## ⚠ 한 줄로 난 길은 고리로 돌면 안 됩니다

끝에서 처음으로 뛰면 **길 밖으로 질러갑니다.** 되짚도록 두었고(`loop = false`),
끝에서 방향이 뒤집히는 것을 테스트가 지킵니다.

## ⚠ 퇴근하면 자리를 처음으로 되돌립니다

안 그러면 다음 날 출근했을 때 어제 쉬던 중간부터 이어져, 한 자리를 건너뛰거나
그 자리에서 다시 한참 서 있습니다.

## 놓인 것

| | 값 |
|---|---|
| 자리 | 4 곳 · 60 m 간격 · 180 m |
| 방향 | 스파인 축을 따라 (없으면 로봇이 보는 쪽) |
| 되짚기 | 예 (한 줄 길) |
| 한 자리에 서는 시간 | 8 초 |
| 시각표 | **18 시 ~ 6 시** |

⚠ **시각은 고른 것이지 잰 것이 아닙니다.** 플레이어가 운전하는 시간대에 길에서
마주치라는 뜻이고, 놀아 보고 고칠 값입니다.

## 검사

```bash
Unity.exe -batchmode -nographics -executeMethod RobotPatrolSetup.Run    # 놓기
Unity.exe -batchmode -nographics -executeMethod RobotPatrolSetup.Check  # 걸을 수 있는가
```

검사가 보는 것은 **씬에 놓인 것**입니다 — 자리가 땅에 붙어 있는가, 자리 사이가
넘을 수 있는 경사인가, 로봇이 길에서 멀지 않은가. 판단 자체는 `RobotPatrolTests`
열 개가 지킵니다.

지금 결과 — 자리 4 곳 · 가장 긴 구간 60 m · 가장 가파른 경사 **7%** ·
로봇에서 가장 가까운 자리 30 m · 문제 0 건. EditMode 278 개 통과.

## 남은 것

* **격납고** — 퇴근하면 지금은 선 자리에 섭니다. 돌아갈 곳이 아직 없습니다.
* **세이브** — 순찰 자리 번호는 저장하지 않습니다. 불러오면 길 처음부터입니다.
* **배치(1번)** — 씬에 스트라이더 하나, 드레드노트 하나뿐입니다.
  장소별·시드 고정 배치는 아직입니다.
* **지각(3번)** — 다음 차례입니다.


---

# 실행 — 지각 (2026-09-11)

작업 순서 3번입니다. **규칙 하나가 이 부품의 모양을 정했습니다** —
"귀신은 당신을 노립니다. **로봇은 당신을 모릅니다.**"

그래서 만든 것은 추적이 아니라 **반응**입니다. 셋뿐이고 **무시가 기본값**입니다.

| 반응 | 언제 | 무엇을 하는가 |
|---|---|---|
| **무시** | 거의 언제나 | 제 일을 계속합니다 |
| **알아챔** | 가까이(26 m) 앞(75도)에 있고 가려지지 않았을 때 | 그뿐입니다 |
| **항의** | **일을 못 하게 됐을 때** | 서서 돌아보고 항의합니다 |

## 항의가 이 설계의 핵심입니다

로봇이 반응하는 이유는 "적을 봤다" 가 **아닙니다.** "가야 하는데 못 간다" 입니다.

```
갈 곳이 있고 + 3.5 초 넘게 못 가고 있고 + 사람이 14 m 안에 있으면 → 항의
```

차를 길 한복판에 세워 두면 항의를 듣고, 비켜 주면 조용해집니다. 쫓기는 것이 아니라
**길을 두고 다투는 것**이라, 앙크가 상대하는 귀신과 축이 겹치지 않습니다.

⚠ **바위에 걸린 것을 사람에게 항의하지 않습니다.** 사람이 멀면 사람 탓이 아닙니다.
⚠ **갈 곳이 없으면 막힐 일도 없습니다.** 퇴근해서 선 기계는 옆에 붙어도 항의하지 않습니다.
⚠ **참는 시간을 짧게 주면 안 됩니다.** 보행기는 한 걸음 사이에 잠깐 멈춥니다.
그 순간을 세면 아무도 앞에 없는데 항의합니다.

## 규칙 하나를 숫자로 지킵니다

테스트 하나가 상황 2000 개를 아무렇게나 흩뿌려 놓고 **항의가 5% 를 넘지 않는지**
봅니다. 넘으면 기계가 사냥꾼처럼 보입니다 — 이 문서의 첫 규칙이 무너지는 자리라
숫자로 못 박았습니다.

## 항의하는 동안 길을 멈춥니다

`RobotPatrol.Paused` 를 켭니다. 안 멈추면 항의하면서 걸어가
**"비켜 달라" 가 "밀고 지나간다" 가 됩니다.**

⚠ 퇴근과 달리 이것은 잠깐 서는 것이므로 **자리 번호와 쉬는 시간은 그대로 둡니다.**
풀리면 가던 자리로 이어서 갑니다.

## 맞으면 둘 다 반응합니다

`RobotCombatant.TakeDamage` 가 `RobotThreat.Provoke` 와 `RobotAwareness.Bumped` 를
함께 부릅니다. 저쪽은 **싸움을 열고** 이쪽은 **서서 항의합니다** —
안 싸우는 기계에게는 이것이 전부입니다.

## 붙은 곳

지각은 **둘 다** 답니다. 순찰기에게는 "길을 막았다" 를 말할 입이 되고, 파수꾼에게는
싸움 앞 단계가 됩니다. 순찰로는 스트라이더만 받습니다.

## 검사

EditMode 287 개 통과 (지각 9 개, 순찰 10 개).

## 알아챔이 보이게 — 고개

처음에는 "알아챔" 이 **아무것도 안 했습니다.** 반응을 셋으로 갈라 놓고 그중 하나가
화면에 아무 흔적도 안 남기면, 있으나 마나입니다.

`RobotTurret.AimAt` 으로 고개가 따라갑니다. **그뿐입니다** — 멈추지도 다가오지도
않습니다. 지나가는 것을 한 번 보는 정도이고, 그 이상을 하면 "로봇은 당신을 모릅니다"
가 깨집니다.

⚠ **싸움이 이깁니다.** `RobotThreat` 도 같은 고개를 겨누므로 그쪽이 깨어 있는 동안에는
여기서 손대지 않습니다. 둘이 같이 쓰면 한 프레임씩 번갈아 겨눠 **고개가 뜁니다.**

## ⚠ 판단 안에 죽은 줄이 있었습니다

고개를 붙이고 다시 읽다가 찾았습니다. `Judge` 첫 줄이 이랬습니다.

```csharp
// 맞았으면 보든 안 보든 항의합니다.
if (sense.SinceHit < 0f) return Response.Ignore;
```

**주석은 맞고 코드는 반대였으며, 조건은 절대 참이 되지 않습니다**(`SinceHit` 은 음수가
안 됩니다). 실제 기억은 `Apply` 에서 따로 다루고 있었으므로 동작은 맞았지만,
그 규칙이 **테스트가 닿지 않는 곳**에 있었습니다.

기억을 `Judge` 안으로 옮기고 테스트 둘을 붙였습니다 — 맞으면 보든 안 보든 항의할 것,
그 기억이 시간이 지나면 풀릴 것.

## 남은 것

* **소리** — `onProtested` 가 열려 있지만 아직 비어 있어서 **항의가 안 들립니다.**
  경적 같은 CC0 소리를 찾아야 합니다.
* **배치(1번)** — 여전히 씬에 둘뿐입니다.


---

# 실행 — 배치 (2026-09-11)

작업 순서 1번입니다. "세계에 없으면 나머지가 전부 무의미합니다."

## 스포너가 아닙니다

기획이 못 박아 두었습니다 — "로봇은 **장소에 속합니다.** 씨앗으로 배치하지,
차 뒤에 뽑아내지 않습니다." 그래서 에디터 도구가 **씬에 구워 넣습니다.**
귀신은 플레이어를 따라다니지만 기계는 **거기 있던 것**이어야 합니다.
그래야 "아홉 시에 저 길을 지나간다" 를 외울 수 있습니다.

시드는 `WorldStreamer.layoutSeed` 와 같은 규칙입니다 — 숫자가 같으면 같은 세계가
나옵니다. ⚠ 시드를 고정한 뒤 `Random.state` 를 **되돌립니다.** 안 되돌리면 이 도구를
돌린 뒤의 모든 난수가 달라집니다.

⚠ **찾는 순서를 이름으로 세웁니다.** `FindObjectsByType` 의 순서는 보장되지 않아서,
그대로 쓰면 같은 시드에서 다른 배치가 나옵니다.

## ⚠ 현장(Site)이 세계에 없습니다

훑어보니 씬의 장소는 **마트**와 **집** 둘뿐이고 **둘 다 `Landmark`** 입니다.

```
ROBOTS 장소 — 마트 · Landmark · 반경 10 m
ROBOTS 장소 — 집   · Landmark · 반경 8 m
```

드레드노트는 **현장을 지키는** 기계인데 지킬 현장이 선언되어 있지 않습니다.
그래서 이번 배치에서 **파수꾼은 0 마리**입니다. 이것은 로봇 쪽 문제가 아니라
**세계가 아직 현장을 선언하지 않은 것**이므로, 여기서 지어내지 않았습니다.

순찰기는 놓았습니다 — `WorldStreamer` 의 길 둘(북쪽 8 타일, 동쪽 6 타일)에
한 마리씩, 길의 중간쯤에.

## ⚠ 검사 도구가 제가 넣은 버그를 잡았습니다

처음 배치에서 북쪽 길의 순찰기가 **y=200** 에 섰습니다. 땅을 찾는 레이캐스트를
`~0` 으로 두어 **메가스트럭처 데크**에 맞은 것입니다. 순찰로는 그 아래 지형에
깔렸으므로 로봇이 **제 길에서 183 m 떨어져** 있었습니다.

```
PATROL ⚠ 로봇이 길에서 183 m 떨어져 있습니다
```

3차에서 만든 `RobotPatrolSetup.Check` 가 바로 그것을 위해 둔 검사였고, 실제로
잡았습니다. 로봇이 걸을 수 있는 땅은 그 로봇의 `RobotDriver.groundMask` 가
정합니다 — 배치와 길이 **서로 다른 세계를 보면 안 됩니다.**

## ⚠ 다시 놓으면 길이 쌓였습니다

로봇을 다시 놓으면 이름이 같은 길 폴더가 그대로 남아, 새 자리가 **옛 자리 뒤에
쌓였습니다.** 순찰로가 두 배 길이가 되고 가운데가 끊깁니다. 폴더를 비우고
다시 놓도록 고쳤습니다.

## 지금 세계에 선 기계

| 로봇 | 어디 | 무엇 |
|---|---|---|
| 스트라이더 (손으로 놓은 것) | 스파인 옆 | 순찰 · 18~6시 |
| 스트라이더 (북쪽 길) | (0, 19, 479) | 순찰 · 18~6시 |
| 스트라이더 (동쪽 길) | (442, 19, 0) | 순찰 · 18~6시 |
| 드레드노트 (손으로 놓은 것) | 스파인 옆 | 파수 · 건드리면 |

검사 — 길 3 개 · 문제 0 건 · 가장 가파른 경사 7%. EditMode 289 개 통과.

## 남은 것

* **현장 선언** — `LocationKind.Site` 인 장소가 생기면 파수꾼이 저절로 붙습니다.
  도구는 이미 그 자리를 비워 두고 있습니다.
* **노마드** — 스파인 순회는 아직입니다. 길 위가 아니라 구조물 위를 걸어야 합니다.
* **소리** — 항의가 아직 안 들립니다.


---

# 실행 — 정비 팔 (2026-09-11)

작업 순서 4번입니다. **차를 고쳐 주는 것이 하나도 없었습니다.**
귀신이 내구도를 깎는데 돌아올 길이 없으면 그 수치는 자원이 아니라 **시계**입니다.

마을이 거점이 되는 이유이기도 합니다. 상점과 침대는 있었지만 **돌아갈 이유**가
없었습니다. 고쳐 주는 기계가 있으면 돌아가는 일에 뜻이 생기고, 돈이 처음으로
**아픈 곳**에 쓰입니다.

## 다리가 없습니다

기획대로 받침대에 `RobotTurret` 한 단을 올렸습니다 — 상자 넷과 회전 마디 둘.
보행 리그가 필요 없어서 "만드는 비용 낮음" 이라고 적혀 있던 그대로입니다.
도구가 프리팹을 만들므로 다음에 무엇이 들어 있었는지 알 수 있습니다.

⚠ **콜라이더는 뿌리에만 답니다.** 조각마다 달면 조준점이 어느 것에 걸리느냐에 따라
부모 탐색이 달라집니다. 하나만 두면 늘 같은 것이 잡힙니다.
⚠ **레이어 6(Interactable) 이 아니면 조준점이 안 걸립니다.**

## 값 매기는 규칙이 유일한 판단입니다

| 규칙 | 왜 |
|---|---|
| 깎인 만큼 받는다 (내구도 1 당 3) | ⚠ 정액이면 **다 부서질 때까지 기다리는 것이 이득**이 되어 조심해서 모는 쪽이 손해를 봅니다 |
| 손을 대면 출장비 15 | 긁힌 자국 하나에 3 원을 받으면 기계가 아니라 자판기처럼 보입니다 |
| **멀쩡하면 한 푼도 안 받는다** | ⚠ 출장비만 받으면 멀쩡한 차에 "정비 15" 가 계속 떠서, 정작 고쳐야 할 때 그 표시를 **안 믿게 됩니다** |
| 반토막은 올려 받는다 | 소수점으로 깎아 주면 긁힌 자국이 영원히 안 고쳐집니다 |
| 연료도 같이 (1 당 2) | 값을 0 으로 두면 주유를 안 합니다 |

⚠ **먼저 받고 고칩니다.** 고치고 받으면 돈이 모자랄 때 **공짜로 고쳐 준 뒤**
거절하게 됩니다.

테스트 일곱 개가 이 규칙들을 지킵니다.

## 곁가지 — `Powertrain.MaxFuel`

바깥에서 "얼마나 비었는가" 를 알 길이 없었습니다. `CurrentFuel` 만 보이고 가득이
얼마인지는 안 보였습니다. 읽기 전용 속성 하나를 더했습니다.

## 놓은 곳

마을 가장자리, 중심에서 39 m. 한복판은 상점과 침대가 쓰고 있고 **차를 대야 하는
기계**라 자리가 필요합니다.

상호작용 외곽선이 자동으로 따라왔습니다 — 외곽선 설치 도구가 "선이 켜질 수 있는
재질" 을 상호작용 대상에서 모으므로, 정비 팔이 생기자 그 재질 둘이 켜졌습니다
(9 개 → 12 개). ⚠ 그만큼 로봇 렌더러에 드로우콜이 하나씩 붙습니다. 로봇은 넷뿐이라
괜찮지만, 나무처럼 많은 것에 이 재질을 쓰면 안 됩니다.

EditMode 296 개 통과.

## 남은 것

* **소리** — 고치는 동안 나는 소리가 없습니다.
* **점등기(6번)** — 로봇과 귀신이 여전히 서로 무관합니다.

---

# 실행 — 견인기 (2026-09-11)

`TowRig` · `TowRigSetup` · 검사 10 개.

## 실패에 뒤를 만듭니다

연료가 0 이 되고 내구도가 0 이 되면 **그다음이 없었습니다.** 길 한복판에서 차가
멈추면 남는 선택은 불러오기뿐이고, 그러면 실패가 **사건이 아니라 되감기**가 됩니다.

이제 순서가 생깁니다 — 기다리고, 값을 치르고, 마을로 끌려가고, 거기서 정비 팔에
**다시** 값을 치릅니다. 두 기계가 이어져 처음으로 한 줄이 됐습니다.

## 판단은 둘뿐입니다

| 무엇 | 규칙 |
|---|---|
| 언제 오는가 | 내구도나 연료가 **바닥나면**. 둘 중 하나면 됩니다 |
| 얼마를 받는가 | 출장비 40 + 끌고 갈 거리 100 m 당 6 |

⚠ **거리로 받습니다.** 정액이면 마을 앞에서 서나 세계 끝에서 서나 같은 값이 되어,
멀리 나가는 일에 값이 매겨지지 않습니다. 멀리 나가는 것이 이 게임의 내용입니다.

둘 다 씬도 차도 지갑도 없이 확인할 수 있게 `static` 으로 꺼내 두었습니다.

## ⚠ 거절하지 않습니다

돈이 모자라면 **가진 것을 다 가져가고 그래도 끌고 갑니다.** 거절하면 플레이어가
길 한복판에 영영 남고, 그때는 불러오기 말고 길이 없어 이 기계를 만든 이유가
사라집니다. 기계는 흥정하지 않습니다.

## ⚠ 가장 가까운 차가 아니라 가장 가까운 멈춰 선 차

처음에는 `Vehicle.FindNearest` 하나로 봤습니다. 그런데 마을에 세워 둔 멀쩡한 차가
늘 더 가까워서 **길에 선 차를 영영 못 봤습니다.** `Vehicle.All` 을 훑어 조건에 맞는
것 중 가장 가까운 것을 고릅니다.

## ⚠ 연료통이 없는 차는 기름으로 서지 않습니다

`CarData` 가 없으면 `MaxFuel` 이 0 이고 `CurrentFuel` 도 0 입니다. 그것을 그대로
읽으면 **멀쩡한 차가 늘 빈 차로** 보여 기계가 끝없이 끌러 옵니다. 용량이 0 이면
볼 수치가 아닌 것으로 다룹니다 — 검사가 그것을 먼저 잡았습니다.

## 놓은 곳

마을 어귀(중심에서 49 m), 정비 팔 반대쪽입니다. 내려놓는 자리는 **정비 팔 옆**
입니다 — 끌려온 차가 고칠 수 있는 자리에 놓여야 그다음이 이어집니다.

⚠ **비무장입니다.** 기획대로이고, 무장한 채로 두면 이 기계가 위협이 되어
"주우러 오는 것" 이라는 뜻이 흐려집니다. 무장 6 개를 껐고 갈고리만 남겼습니다.

스트라이더에는 이미 `Gun_TowHook` 자리와 `WeaponWinch` 가 배선되어 있어서
**새로 만든 모델이 없습니다.**

## 알아보는 거리 400 m

길은 마을에서 뻗어 나가므로, 어귀에 선 기계가 멀리 볼수록 **모든 길**을 그만큼씩
덮습니다. 좁게 두면 길 하나에 한 마리씩 세워야 하고, 걷는 기계는 다리가 비쌉니다.

⚠ **그보다 멀리서 서면 아무도 오지 않습니다.** 지금은 그렇습니다. 무전으로 부르는
길을 열거나, 기계를 길마다 세우거나, 둘 중 하나가 다음에 필요합니다.

## 검사

* EditMode 6 개 — 언제 부르는가 · 얼마를 받는가.
* PlayMode 4 개 — 걷는 부품을 빼고 **순서**만 잽니다. 알아채고, 걸고, 값을 받고,
  놓습니다. 다리가 목적지에 닿는지는 걷는 부품의 몫입니다.

EditMode 309 개, PlayMode 63 개 전부 통과.

## 남은 것

* **끌려가는 동안의 플레이어** — 지금은 차가 기계 뒤 5 m 에 떠서 따라옵니다.
  타고 있으면 같이 갑니다. 걸어서 따라가는 쪽이 그림이 더 나을지는 정하지 않았습니다.

---

# 실행 — 등과 점등기 (2026-09-11)

`StreetLamp` · `LampLighter` · `StreetLampSetup` · 검사 16 개.

## 어두워지면 켜집니다

밝기가 0.35 아래로 내려가면 켜지고 0.5 위로 올라가야 꺼집니다.

⚠ **문턱이 하나면 깜빡입니다.** 해 질 녘 밝기가 문턱 언저리에서 떨리면 등이
깜빡거립니다. 끄는 문턱을 위로 벌려 두어야 한 번 켜진 등이 붙어 있습니다.

⚠ **시간을 직접 읽지 않습니다.** `IGameClock` 이 밝기를 압니다. 해의 각도를 여기서
또 계산하면 하늘과 등이 서로 다른 시각을 믿게 됩니다.

⚠ **프리팹은 꺼진 채로 굽습니다.** 켜고 끄는 것은 재생 중에만 도는 `Update` 라,
켜진 채로 저장하면 에디터의 씬 뷰에서도 룩 캡처에서도 등 열여섯 개가 대낮에
타고 있습니다 — 실제로 한밤 운전석 밝기가 0.006 에서 0.111 로 뛰었습니다.

## ⚠ 등이 땅을 못 밝히고 있었습니다

세운 등이 **자기 팔만 하얗게 태우고 땅에는 아무것도 못 했습니다.** 세기를 3 에서
300 까지 올려 봐도 등 아래 땅이 0.033~0.035 로 꿈쩍하지 않았습니다.

원인은 툰 조명이 **추가 광원의 거리 감쇠까지 밴드로 끊고** 있던 것이었습니다.
점광원의 감쇠는 1/d² 이라 `ToonBand(감쇠, 0.5)` 를 넘는 곳이 **반경 1.4 m 뿐**입니다.
그 밖은 세기를 아무리 올려도 0 입니다.

**헤드라이트가 길을 못 밝히던 것도 같은 이유였습니다.** 밤 주행 게임인데
헤드라이트가 1.4 m 앞까지만 닿고 있었습니다.

명암 경계(N·L)는 그대로 끊고 **거리 감쇠만 끊지 않도록** 고쳤습니다
(`ToonExtraLightAmount`). 그 뒤 값입니다.

| 화면 | 전 | 후 |
|---|---|---|
| 한밤 바깥 | 0.024 | 0.080 |
| 한밤 운전석 | 0.003 | 0.087 |
| 한낮 바깥 | 0.262 | 0.297 |

한밤 운전석이 서른 배 밝아 보이지만 계기판이 밝아진 것이 아니라 **헤드라이트가
길을 비추기 시작한 것**입니다. 세기는 재서 골랐습니다 — 등 아래 땅이
3 → 0.07, 20 → 0.18, 80 → 0.39, 300 → 0.74 이고, 세계의 한밤 평균이 0.024 이므로
18 이 웅덩이가 보이되 타지 않는 자리입니다.

## 점등기

길을 걸으며 죽은 등을 되살립니다. 판단은 하나뿐입니다 — **죽었고, 찾는 범위
안에 있는가.** 300 m 안의 가장 가까운 죽은 등으로 갑니다.

⚠ **낮에도 걷습니다.** 등은 어두워져야 켜지지만 고치는 일은 밝을 때 하는 편이
낫습니다. 밤에만 일하면 플레이어가 이 기계를 만나는 시간이 곧 귀신을 만나는
시간과 겹쳐, 지킬 여유가 없습니다.

⚠ **비무장입니다.** 지키고 싶어져야 하는 기계가 총을 들고 있으면 그 뜻이 흐려집니다.

## 로봇과 귀신이 처음으로 곱해집니다

`GhostSpawner` 가 스폰 간격에 `Calm(그 자리의 밝기, 2.5)` 를 곱합니다. 등 바로
아래에서 간격이 2.5 배로 벌어집니다.

⚠ **0 으로 만들지 않습니다.** 등 아래가 완전한 안전지대가 되면 밤에 등 밑에 차를
대고 기다리는 것이 최적 전략이 됩니다. 뜸해질 뿐입니다.

⚠ **가장 밝은 등 하나만 셉니다.** 여러 등을 더하면 등이 촘촘한 마을이 1 을 훌쩍
넘어 귀신이 아예 안 나옵니다. 마을이 안전한 것과 마을에 아무 일도 안 일어나는
것은 다릅니다.

## 놓은 것

길마다 80 m 간격, 좌우 번갈아 16 개. 그중 **4 개는 죽은 채**입니다 — 죽은 등이
없으면 점등기가 할 일이 없습니다. 점등기는 마을에서 120 m 나간 길 위에 섭니다.

## 소리

없던 소리를 채웠습니다. 새 소리를 구하지 못해 **있던 충돌음을 씁니다** —
쇳덩이가 쇳덩이에 닿는 소리라 그대로 쓸 수 있었습니다.

| 무엇 | 언제 |
|---|---|
| 정비 팔 | 손을 댈 때 · 다 고쳤을 때 · 돈이 모자랄 때 |
| 견인기 | 갈고리가 걸릴 때 · 내려놓을 때 |
| 등 | 켜지고 꺼질 때 |
| 점등기 | 등을 살렸을 때 |

## 검사

* EditMode 12 개 — 언제 켜지는가(히스테리시스) · 고치러 갈 등인가 · 등불이 귀신을
  얼마나 미루는가.
* PlayMode 5 개 — 등이 실제로 켜지고 꺼지는가 · 죽은 등은 안 켜지는가 ·
  등 아래가 밝은가 · 점등기가 되살리는가. 그리고 **진짜 씬에서** 밤이 되면
  켜지는지 한 장 찍어 둡니다(`Logs/Lamp/night.png`).

EditMode 321 개, PlayMode 69 개 전부 통과.

## 등의 세이브 (2026-09-11)

`LampSaveParticipant` · `SaveData.lamps` · 검사 6 개.

점등기가 밤새 되살린 등이 **불러오기 한 번에 도로 죽고** 있었습니다. 그러면 그
기계가 한 일이 없던 일이 되고, 플레이어가 지켜 준 밤도 없던 일이 됩니다 —
쓰러뜨린 로봇이 쓰러진 채 남아야 하는 것과 같은 이유입니다.

**담는 것은 "죽었는가" 하나뿐입니다.** 자리는 배치 시드가 정하고 켜짐은 시각이
정합니다. 지형을 담지 않는 것과 같은 이유로, 스스로 정해지는 것은 담지 않습니다.

⚠ **짝이 없는 등은 건드리지 않습니다.** 배치 시드를 바꾼 뒤의 옛 세이브는 이름이
어긋나는데, 그때 전부 죽이거나 전부 살리면 **세이브에 없던 결정**을 참여자가
내리는 셈입니다. 놓인 그대로 두고 경고만 남깁니다.

⚠ **`broken` 을 직접 쓰지 않고 `Break`·`Relight` 로 넘깁니다.** 필드를 직접 쓰면
이미 타고 있던 등이 죽은 채로 계속 빛납니다.

참여자는 씬에 없습니다 — `GameBootstrap` 이 기동할 때 만듭니다. 그 배선이 빠지면
저장은 성공하는데 등만 조용히 빠지므로, **진짜 씬에서** 열여섯 개가 담기는지
한 번 더 봅니다.

EditMode 321 개, PlayMode 74 개 전부 통과.

## 점등기의 시각표 (2026-09-11)

7시에 출근해 18시에 퇴근합니다. 퇴근하면 **마을 한쪽**으로 돌아가 섭니다.

**낮에 일하는 이유**는 둘입니다. 등을 고치는 일은 보이는 때에 하는 것이고,
이 기계는 셋 중 유일하게 **지키고 싶어져야 하는** 기계라 플레이어가 그것을
만나는 시간이 귀신을 만나는 시간과 겹치면 안 됩니다.

**두 가지 모습으로 만납니다.** 낮에는 길에서 일하는 모습으로, 밤에는 마을에 선
모습으로. 한 기계를 한 모습으로만 만나면 그것은 장치이지 사는 물건이 아닙니다.

⚠ **시각표를 일보다 먼저 봅니다.** 손보던 중에 퇴근 시각이 되면 그 등은 두고
갑니다. 하던 일을 마치고 가게 두면 퇴근이 일에 밀려 시각표가 있으나 마나입니다.

⚠ **퇴근할 때 하던 일을 되돌립니다.** 안 그러면 다음 날 출근했을 때 어제 손보던
중간부터 이어져, 이미 켜진 등 앞에서 한참 서 있습니다. 순찰기가 같은 일을 합니다.

## ⚠ 같은 규칙이 세 곳에 적혀 있었습니다

시각 구간을 판단하는 규칙이 **가게의 영업 시간 · 순찰기의 근무 시간 · 점등기의
근무 시간** 세 곳에 글자까지 같게 적혀 있었습니다. 그런 것은 한쪽만 고쳐지는
날이 옵니다. `DayHours.Within` 하나로 모으고 셋이 그것을 부릅니다.

규칙은 그대로입니다 — 시작은 넣고 끝은 뺍니다(8~18시 근무면 18시 정각은 퇴근),
시작과 끝이 같으면 하루 종일, 시작이 끝보다 늦으면 자정을 넘긴 구간입니다.
검사 4 개를 그 한 곳에 붙였습니다.

EditMode 325 개, PlayMode 76 개 전부 통과.

## 남은 것

* **점등기가 마을에 서 있는 동안** — 아무것도 하지 않습니다. 정비 팔처럼 말을
  걸 수 있게 하거나, 밤새 켜진 등을 세는 정도의 일거리는 줄 수 있습니다.
