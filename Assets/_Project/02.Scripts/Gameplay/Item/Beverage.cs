using UnityEngine;

/// <summary>
/// 이 컴포넌트를 가진 GameObject를 '음료'로 식별합니다.
/// BeverageBox 스크립트가 이 컴포넌트를 찾아 리스트에 추가합니다.
/// 내부에 별도 코드가 필요 없습니다. (태그 역할)
/// </summary>
public class Beverage : MonoBehaviour
{
    // 이 스크립트는 식별용 태그 역할만 합니다.
    // Start(), Update() 등 Unity 이벤트 함수가 필요 없습니다.
}
